VITE_NETWORK_ID=undeployed
VITE_INDEXER_URL=http://localhost:8088/api/v4/graphql
VITE_INDEXER_WS_URL=ws://localhost:8088/api/v4/graphql/ws
VITE_PROOF_SERVER_URL=http://localhost:6300
VITE_NODE_URL=http://localhost:9944
VITE_NODE_WS=ws://localhost:9944
VITE_DEFAULT_CONTRACT=
VITE_MOVE_NFT_CONTRACT=
VITE_MUSDC_CONTRACT=
VITE_MANDATE_CONTRACT=
VITE_ORDER_CONTRACT=
VITE_TREASURY_LABEL=streetrail:treasury:v1

# Server-only — AIsa powers /agent-negotiation (never prefix with VITE_)
AISA_API_KEY=

# Server-only — Pinata IPFS for /moves clip + metadata pinning (never prefix with VITE_)
# Create at https://app.pinata.cloud → API Keys → pinFileToIPFS + pinJSONToIPFS
PINATA_JWT=
# Optional branded gateway host (e.g. mygateway.mypinata.cloud) — used for gateway URLs only
PINATA_GATEWAY=
